# forge build output and the vendored test dependency; install with
#   forge install foundry-rs/forge-std --no-git
contracts/out/
contracts/lib/
cache/
# per-run evidence is regenerated by pocs/run_all.py; EVIDENCE.md is the committed artifact
pocs/__pycache__/
