[package]
name = "packet-storm-flooder"
version = "0.1.0"
edition = "2024"

[dependencies]
ethrex-p2p = { path = "../../../crates/networking/p2p" }
ethrex-common = { path = "../../../crates/common" }
tokio = { version = "1", features = ["full"] }
rand = "0.8"
secp256k1 = { version = "0.30", default-features = false, features = ["global-context", "recovery", "rand"] }
