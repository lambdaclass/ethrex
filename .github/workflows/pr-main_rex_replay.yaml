name: Workflow Call Replay

on:
  push:
    branches: ["main"]
  pull_request:
    types: [opened, synchronize, reopened]
    branches: ["**"]
  workflow_dispatch:

jobs:
  build-b:
    uses: lambdaclass/ethrex-replay/.github/workflows/test.yaml@workflow_call
    with:
      a_repo: lambdaclass/ethrex
      a_ref: ${{ github.sha }}
