name: Workflow Call Replay
permissions:
  contents: read
  actions: read
  checks: read

on:
  push:
    branches: ["**"] #Just for testing
  pull_request:
    types: [opened, synchronize, reopened]
    branches: ["**"]
  workflow_dispatch:

jobs:
  build-b:
    permissions:
      contents: read
      actions: read
      checks: read
    uses: lambdaclass/ethrex-replay/.github/workflows/pr-main-lint.yaml@move_replay
    with:
      a_repo: lambdaclass/ethrex
      a_ref: ${{ github.sha }}
