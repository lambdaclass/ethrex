name: Workflow Call Replay

on:
  push:
    paths:
      - crates/**
      - ".github/workflows/pr-main_replay.yaml"
  workflow_dispatch:

jobs:
  quick-run:
    uses: lambdaclass/ethrex-replay/.github/workflows/workflow-call.yaml@move_replay
